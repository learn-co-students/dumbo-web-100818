<ul>
<% @dogs.each do |pupper| %>
  <li>
    <!-- <a href="/dogs<"><%= pupper.name %></a> -->
<!--     <%= link_to pupper.name, "/dogs/#{ pupper.id }" %>
    <%= link_to pupper.name, dog_path(pupper.id) %>
    <%= link_to pupper.name, dog_path(pupper) %> -->
    <%= link_to pupper.name, pupper %>

    <%= form_for pupper, method: :delete do |f| %>
      <%= f.submit "DELETE" %>
    <% end %>

  </li>
<% end %>
</ul>

